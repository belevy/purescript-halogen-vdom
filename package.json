{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf",
    "test": "pulp build -I test -- --censor-lib --strict",
    "build": "pulp -w build"
  },
  "devDependencies": {
    "pulp": "^16.0.2",
    "purescript": "0.15.6",
    "rimraf": "^2.6.2",
    "spago": "^0.20.9"
  }
}
